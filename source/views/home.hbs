{{#section "intro"}}
{{> homeIntro}}
{{/section}}
{{#section 'css'}}
<link rel="stylesheet" type="text/css" href="/css/home.css">
{{/section}}

{{#section 'js'}}
<!--script type="text/javascript" src="/js/addons-pro/mdb-file-upload.min.js"></script-->
<script type="text/javascript" src="/js/home.js"></script>
{{/section}}

{{> countdown}}
<!-- Intro -->
<!-- Main Container -->
<div class="container">
      <section >
        <!-- Grid row -->
        <div class="row">
          <!-- Grid column -->
          <div class="col-12">
            <div class="divider-new">
              <h3 class="h3-responsive font-weight-bold dark-grey-text mx-3" id="higlightCourses" style="font-size:1cm">Highlight courses</h3>
            </div>
            <br>
            <!-- Grid row -->
            <div class="row">
              {{#if special_courses}}
              {{#each special_courses}}
              <div class="col-lg-4 col-md-12 mb-4">
                <!-- Card -->
                <div class="card card-ecommerce4">
                  <!-- Card image -->
                  <div class="view overlay zoom">
                    <img src="{{image}}" class="img-fluid" alt="Not image">
                    <a href="/course/{{id}}">
                      <div class="mask rgba-white-slight"></div>
                    </a>
                  </div>
                  <!-- Card image -->
                  <!-- Card content -->
                  <div class="card-body">
                    <!-- Category & Title -->
                    <h5 class="card-title mb-1">
                      <strong>
                        <a href="/course/{{id}}" class="dark-grey-text">{{title}}</a>
                      </strong>
                    </h5>
                    <span class="badge badge-info mb-2">Best seller</span>
                    <script>
                      if ({{ discount }} > 0)
                      document.write('<span class="badge badge-danger mb-2 ml-2">SALE</span>');
                    </script>
                    <!-- Rating -->
                    <h5 class="h5-responsive text-center text-md-left ml-xl-0 ml-4">
                      <span>
                        <script>
                          var point =  {{avgStar}};
                          for (var j = 1; j <= 5; j++) {
                            if (point >= 1) {
                              document.write('<i class="fas fa-star orange-text"></i>');
                              point -= 1;
                              continue;
                            }
                            if (point > 0) {
                              document.write('<i class="fas fa-star-half orange-text"></i>');
                              point -= 1;
                              continue;
                            }
                            document.write('<i class="far fa-star orange-text"></i>');
                            point -= 1;
                          }
                        </script>
                      </span>
                      <span class="card-text">({{numRating}} ratings) {{numStudent}} students</span>
                    </h5>
                    <p class="mb-3 text-muted text-uppercase small word-wrap-one-line">By {{instructor.fullname}}</p>
                    <span class="mb-0 p-0">
                      <span class="chip chip-sm small mb-0 pb-0">
                        <a href="/course/byCat/?category_id={{category.id}}">{{{category.icon}}} {{category.name}}</a>
                      </span>
                      <span class="chip chip-sm small mb-0 pb-0">
                        <a href="/course/byCat/?sub_category_id={{sub_category.id}}">{{{sub_category.icon}}} {{sub_category.name}}</a>
                      </span>               
                    </span>
                    <hr>
                      <h5 class="h5-responsive text-center text-md-left">
                        {{#is discount 0}}
                        <span class="red-text font-weight-bold">
                          <strong>{{this.full_price}} </strong>
                          <small>
                            $
                          </small>
                        </span>
                        {{else}}
                        <span class="grey-text">
                          <s>
                            <strong>{{this.full_price}} </strong>
                            <small>
                              $
                            </small>
                          </s>
                          <span class="red-text font-weight-bold">
                            <strong>{{this.price}} </strong>
                            <small>
                              $
                            </small>
                          </span>
                        </span>
                        {{/is}}
                        {{#if ../session/username}}
                        <span class="float-right">
                          {{#if (eq ../session.type "student")}}
                          {{#if isBuy}}
                          <a class="" data-toggle="tooltip" data-placement="top" title="Was buy">
                            <i class="fas fa-check ml-3"></i>
                          </a>
                          {{else}}
                          {{#if inCart}}
                          <a class="" data-toggle="tooltip" data-placement="top" title="In Cart" href="/cart">&nbsp;&nbsp;
                            <i class="fas fa-luggage-cart"></i>
                          </a>
                          {{else}}
                          <a class="" id="addToCard{{id}}" data-toggle="tooltip" data-placement="top" title="Add to Cart"
                            onclick="addToCartFunction({{id}});">
                            <i class="fas fa-cart-plus"></i>
                          </a>
                          {{/if}}
                          {{/if}}
                          {{/if}}
                          {{#if (eq ../session.type "administrator")}}
                          {{#if disable}}
                          <a class="" id="admin1{{id}}" data-toggle="tooltip" data-placement="top" title="Undisable"
                            onclick="undisable({{id}})">
                            <i class="fas fa fa-unlock"></i>
                          </a>
                          {{else}}
                          <a class="" id="admin1{{id}}" data-toggle="tooltip" data-placement="top" title="Disable" onclick="disable({{id}})">
                            <i class="fas fa fa-ban"></i>
                          </a>
                          {{/if}}
                          {{/if}}
                          {{/if}}
                        </span>
                      </h5>
                  </div>
                  <!-- Card content -->
                </div>
                <!-- Card -->
              </div>
              {{/each}}
              {{/if}}
            </div>
          </div>
          <!-- Grid row -->
      </section>
      <!-- Section: Bestsellers & offers -->
      <!-- Section: Advertising -->
      <section>
        <!-- Grid row -->
        <div class="row">
          <!-- Grid column -->
          <div class="col-12">
            <!-- Image -->
            <div class="view z-depth-1 zoom">
              <img style="width: 480px;height: 270px;" src="https://img-a.udemycdn.com/course/750x422/293814_7732_4.jpg"
                class="img-fluid" alt="sample image">
              <div class="mask rgba-stylish-slight">
                <div class="dark-grey-text text-right pt-lg-5 pt-md-1 mr-5 pr-md-4 pr-0">
                  <div>
                    <h2 class="card-title font-weight-bold pt-md-3 pt-1">
                      <a>
                        <span class="badge badge-danger">SALE</span>
                      </a>
                      <h1 class="font-weight-bold"><a  href="/course/8">DEAL SHOCK</a>
                      </h1>
                    </h2>

                    <h2 class="pb-lg-3 pb-md-1 clearfix d-none d-md-block">Sale up to 80%</h2>

                  </div>
                </div>
              </div>
            </div>
            <!-- Image -->
          </div>
          <!-- Grid column -->
        </div>
        <!-- Grid row -->
      </section>
      <!-- Section: 10 khóa học được xem nhiều nhất -->
      <section id="relatedCourses">
        <div class="divider-new">
          <h3 class="h3-responsive font-weight-bold dark-grey-text mx-3" style="font-size:1cm">TOP VIEW COURSE</h3>
        </div>
        <!-- Carousel Wrapper -->
        <div id="multi-item-example1" class="carousel slide carousel-multi-item" data-ride="carousel">
          <!-- Controls -->
          <div class="controls-top">
            <a class="btn-floating primary-color" href="#multi-item-example1" data-slide="prev">
              <i class="fas fa-chevron-left"></i>
            </a>
            <a class="btn-floating primary-color" href="#multi-item-example1" data-slide="next">
              <i class="fas fa-chevron-right"></i>
            </a>
          </div>
          <!-- Controls -->
          <!-- Indicators -->
          <ol class="carousel-indicators">
            <li class="primary-color" data-target="#multi-item-example1" data-slide-to="0" class="active"></li>
            <li class="primary-color" data-target="#multi-item-example1" data-slide-to="1"></li>
            <li class="primary-color" data-target="#multi-item-example1" data-slide-to="2"></li>
          </ol>
          <!-- Indicators -->
          <!-- Slides -->
          <div class="carousel-inner" role="listbox">
            <script>
              var num = 0;
            </script>
            {{#each mostView_courses}}
            <script>
              if (num % 4 === 0) {
                if (num === 0)
                  document.write(`<div class="carousel-item active">`);
                else
                  document.write(`<div class="carousel-item">`);
              }
            </script>
              <div class="col-3 clearfix d-none d-md-block mb-4">
                <!-- Card -->
                <div class="card">

                  <!-- Card image -->
                  <div class="card-img-top view overlay zoom">
                    <img src="{{image}}" class="img-fluid" alt="{{title}}">
                    <a href="/course/{{id}}">
                      <div class="mask flex-center waves-effect waves-light rgba-black-strong">
                        <p class="white-text">
                          {{title}}
                        </p>
                      </div>
                    </a>
                  </div>
                  <!-- Card image -->

                  <!-- Card content -->
                  <div class="card-body">
                    <!-- Category & Title -->
                      <div class="card-title mb-1">
                          <a href="/course/{{id}}" class="dark-grey-text word-wrap-one-line" title="{{title}}">{{title}}</a>
                      </div>
                    {{#each tag}}
                    <span class="badge badge-danger mb-2">{{name}}</span>
                    {{/each}}
                    <!-- Rating -->
                    <ul class="rating"> {{avgStar}}
                      <script>
                        var point = {{ avgStar }};
                        for (var j = 1; j <= 5; j++) {
                          if (point >= 1) {
                            document.write(`<i class="fas fa-star orange-text"></i>`);
                            point -= 1;
                            continue;
                          }
                          if (point > 0) {
                            document.write('<i class="fas fa-star-half orange-text"></i>');
                            point -= 1;
                            continue;
                          }
                          document.write('<i class="far fa-star orange-text"></i>');
                          point -= 1;
                        }
                      </script>
                      <span class="mdb-color-text"> {{total_rating}}</span>
                    </ul>

                    <p class="mb-3 text-muted text-uppercase small word-wrap-one-line">By {{instructor.fullname}}</p>
                    <!-- Card footer -->
                    <hr>
                        <h5 class="h5-responsive text-center text-md-left">
                          {{#is discount 0}}
                          <span class="red-text font-weight-bold">
                            <strong>{{this.full_price}} </strong>
                            <small>
                              $
                            </small>
                          </span>
                          {{else}}
                          <span class="grey-text">
                            <s>
                              <strong>{{this.full_price}} </strong>
                              <small>
                                $
                              </small>
                            </s>
                            <span class="red-text font-weight-bold">
                              <strong>{{this.price}} </strong>
                              <small>
                                $
                              </small>
                            </span>
                          </span>
                          {{/is}}
                      {{#if ../session/username}}
                      <span class="float-right">
                      {{#if (eq ../session.type "student")}}
                        {{#if isBuy}}
                      <a class="" data-toggle="tooltip" data-placement="top" title="Was buy">
                        <i class="fas fa-check ml-3"></i>
                      </a>
                        {{else}}
                          {{#if inCart}}
                          <a class="" data-toggle="tooltip" data-placement="top" title="In Cart" href="/cart">&nbsp;&nbsp;
                            <i class="fas fa-luggage-cart"></i>
                          </a>
                          {{else}}
                          <a class="" id="addToCard{{id}}" data-toggle="tooltip" data-placement="top" title="Add to Cart" onclick="addToCartFunction({{id}});">
                            <i class="fas fa-cart-plus"></i>
                          </a>
                          {{/if}}
                        {{/if}}
                      {{/if}}
                      {{#if (eq ../session.type "administrator")}}
                        {{#if disable}}
                        <a class="" id="admin2{{id}}" data-toggle="tooltip" data-placement="top" title="Undisable" onclick="undisable({{id}})">
                          <i class="fas fa fa-unlock"></i>
                        </a>
                        {{else}}
                        <a class="" id="admin2{{id}}" data-toggle="tooltip" data-placement="top" title="Disable" onclick="disable({{id}})">
                          <i class="fas fa fa-ban"></i>
                        </a>
                        {{/if}}
                      {{/if}}
                      {{/if}}
                      </span>
                    </h5>
                  </div>
                </div>


                <!-- Card -->

              </div>

            <script>
              if (num % 4 === 3)
                document.write('</div>');
              num++;
            </script>
            {{/each}}
          </div>
          <!-- Slides -->
        </div>
        <!-- Carousel Wrapper -->
      </section>
      <!--Section: 10 khóa học được đăng gần nhất -->
      <section id="relatedCourses">
        <div class="divider-new">
          <h3 class="h3-responsive font-weight-bold dark-grey-text mx-3" style="font-size:1cm">NEWEST COURSE</h3>
        </div>
        <!-- Carousel Wrapper -->
        <div id="multi-item-example" class="carousel slide carousel-multi-item" data-ride="carousel">
          <!-- Controls -->
          <div class="controls-top">
            <a class="btn-floating primary-color" href="#multi-item-example" data-slide="prev">
              <i class="fas fa-chevron-left"></i>
            </a>
            <a class="btn-floating primary-color" href="#multi-item-example" data-slide="next">
              <i class="fas fa-chevron-right"></i>
            </a>
          </div>
          <!-- Controls -->
          <!-- Indicators -->
          <ol class="carousel-indicators">
            <li class="primary-color" data-target="#multi-item-example" data-slide-to="0" class="active"></li>
            <li class="primary-color" data-target="#multi-item-example" data-slide-to="1"></li>
            <li class="primary-color" data-target="#multi-item-example" data-slide-to="2"></li>
          </ol>
          <!-- Indicators -->
          <!-- Slides -->
          <div class="carousel-inner" role="listbox">
            <script>
              var num = 0;
            </script>
            {{#each last_courses}}
            <script>
              if (num % 4 === 0) {
                if (num === 0)
                  document.write(`<div class="carousel-item active">`);
                else
                  document.write(`<div class="carousel-item">`);
              }
            </script>
              <div class="col-3 clearfix d-none d-md-block mb-4">
                <!-- Card -->
                <div class="card">

                  <!-- Card image -->
                  <div class="card-img-top view overlay zoom">
                    <img src="{{image}}" class="img-fluid" alt="{{title}}">
                    <a href="/course/{{id}}">
                      <div class="mask flex-center waves-effect waves-light rgba-black-strong">
                        <p class="white-text">
                          {{title}}
                        </p>
                      </div>
                    </a>
                  </div>
                  <!-- Card image -->

                  <!-- Card content -->
                  <div class="card-body">
                    <!-- Category & Title -->
                      <div class="card-title mb-1">
                          <a href="/course/{{id}}" class="dark-grey-text word-wrap-one-line" title="{{title}}">{{title}}</a>
                      </div>
                    {{#each tag}}
                    <span class="badge badge-danger mb-2">{{name}}</span>
                    {{/each}}
                    <!-- Rating -->
                    <ul class="rating"> {{avgStar}}
                      <script>
                        var point = {{avgStar}};
                        for (var j = 1; j <= 5; j++) {
                          if (point >= 1) {
                            document.write(`<i class="fas fa-star orange-text"></i>`);
                            point -= 1;
                            continue;
                          }
                          if (point > 0) {
                            document.write('<i class="fas fa-star-half orange-text"></i>');
                            point -= 1;
                            continue;
                          }
                          document.write('<i class="far fa-star orange-text"></i>');
                          point -= 1;
                        }
                      </script>
                      <span class="mdb-color-text"> {{total_rating}}</span>
                    </ul>

                    <p class="mb-3 text-muted text-uppercase small word-wrap-one-line">By {{instructor.fullname}}</p>
                    <!-- Card footer -->
                    <hr>
                        <h5 class="h5-responsive text-center text-md-left">
                          {{#is discount 0}}
                          <span class="red-text font-weight-bold">
                            <strong>{{this.full_price}} </strong>
                            <small>
                              $
                            </small>
                          </span>
                          {{else}}
                          <span class="grey-text">
                            <s>
                              <strong>{{this.full_price}} </strong>
                              <small>
                                $
                              </small>
                            </s>
                            <span class="red-text font-weight-bold">
                              <strong>{{this.price}} </strong>
                              <small>
                                $
                              </small>
                            </span>
                          </span>
                          {{/is}}
                      {{#if ../session/username}}
                      <span class="float-right">
                      {{#if (eq ../session.type "student")}}
                        {{#if isBuy}}
                      <a class="" data-toggle="tooltip" data-placement="top" title="Was buy">
                        <i class="fas fa-check ml-3"></i>
                      </a>
                        {{else}}
                          {{#if inCart}}
                          <a class="" data-toggle="tooltip" data-placement="top" title="In Cart" href="/cart">&nbsp;&nbsp;
                            <i class="fas fa-luggage-cart"></i>
                          </a>
                          {{else}}
                          <a class="" id="addToCard{{id}}" data-toggle="tooltip" data-placement="top" title="Add to Cart" onclick="addToCartFunction({{id}});">
                            <i class="fas fa-cart-plus"></i>
                          </a>
                          {{/if}}
                        {{/if}}
                      {{/if}}
                      {{#if (eq ../session.type "administrator")}}
                        {{#if disable}}
                        <a class="" id="admin3{{id}}" data-toggle="tooltip" data-placement="top" title="Undisable" onclick="undisable({{id}})">
                          <i class="fas fa fa-unlock"></i>
                        </a>
                        {{else}}
                        <a class="" id="admin3{{id}}" data-toggle="tooltip" data-placement="top" title="Disable" onclick="disable({{id}})">
                          <i class="fas fa fa-ban"></i>
                        </a>
                        {{/if}}
                      {{/if}}
                      {{/if}}
                      </span>
                    </h5>
                  </div>
                </div>


                <!-- Card -->

              </div>

            <script>
              if (num % 4 === 3)
                document.write('</div>');
              num++;
            </script>
            {{/each}}
          </div>
          <!-- Slides -->
        </div>
        <!-- Carousel Wrapper -->
      </section>
 
  
</div>
